
/**
 * Print console log.
 *
 * @param {Object} data
 */
const printLog = (data) => {
    console.log(`Created:\t${data.relativeFileExt}`);
    // console.log(data);
    return data;
};

module.exports = printLog;
